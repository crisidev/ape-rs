[package]
name = "cosmo-sysconf"
version = "0.1.0"
edition = "2021"
description = "Runtime system-config lookups (DNS servers) portable across host OSes under Cosmopolitan / APE."
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
libc = "0.2"
